name: Spack build

on:
  push:
    tags:
      - '*'
    branches:
      - 'main'
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "5 4 * * *"

jobs:
  build_macos:
    name: macos ${{ matrix.os }} · ${{ matrix.compiler }}
    strategy:
      fail-fast: false
      matrix:
        include:

         - os: macos-26
           xcode: "26.3"
           compiler: "apple-clang@17.0.0"
           default: true

    uses: ./.github/workflows/build_one.yml
    with:
      os: ${{ matrix.os }}
      xcode: ${{ matrix.xcode }}
      compiler: ${{ matrix.compiler }}
      default: ${{ matrix.default }}
    secrets: inherit

  build_container:
    name: ${{ matrix.compiler }}${{ matrix.cxxstd == '23' && ' (C++23)' || '' }} · ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:

        include:
          - image: ghcr.io/acts-project/alma9-base:85
            compiler: gcc@15.1.1
            compiler_path: "/opt/rh/gcc-toolset-15/root"
            os: ubuntu-latest
            default: true

          - image: ghcr.io/acts-project/ubuntu2404:85
            compiler: gcc@13.3.0
            os: ubuntu-latest
            default: true

          - image: ghcr.io/acts-project/ubuntu2404_clang22:85
            compiler: llvm@22.1.1
            os: ubuntu-latest
            default: false

          - image: ghcr.io/acts-project/ubuntu2404:85
            compiler: gcc@13.3.0
            os: ubuntu-24.04-arm
            default: false

          - image: ghcr.io/acts-project/alma9-base:85
            compiler: gcc@15.1.1
            compiler_path: "/opt/rh/gcc-toolset-15/root"
            os: ubuntu-latest
            cxxstd: "23"
            default: false

          - image: ghcr.io/acts-project/ubuntu2404_clang22:85
            compiler: llvm@22.1.1
            os: ubuntu-latest
            cxxstd: "23"
            default: false

    uses: ./.github/workflows/build_one.yml
    with:
      os: ${{ matrix.os }}
      image: ${{ matrix.image }}
      compiler: ${{ matrix.compiler }}
      compiler_path: ${{ matrix.compiler_path || '' }}
      cxxstd: ${{ matrix.cxxstd || '20' }}
      default: ${{ matrix.default }}
    secrets: inherit

  merge_container:
    name: Merge and push container manifests
    runs-on: ubuntu-latest
    needs: [build_container]
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - uses: actions/checkout@v6

      - name: Install the latest version of uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "latest"

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build merged manifest and push to regisrty
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          uv run merge_images.py ${{ github.ref_name }} ".*ubuntu24.04.*gcc.*" --push
