version: 2
updates:
  # Keep the SHA-pinned GitHub Actions fresh. zizmor requires hash pins
  # (.github/zizmor.yml), so without this the pins would silently rot.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns:
          - "*"
